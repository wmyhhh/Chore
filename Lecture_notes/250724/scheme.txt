Scheme is a dialect of LISP

we call it procedures instead of functions in Scheme

primitive expressions: 2, 5.8, +, true, quotien
combinations: (not true) (quotien 100 4)

special form
if expression: (if <predicate> <consequent> <alternative>)
and & or: (and <exp1> <exp2> ... ) (or <exp1> <exp2> ...)
binding symbols: (define <symbol> <expression>)
new procedures： (define (<symbol> <formal parametres>) <body>)

(define (abs x)
    if (> x 0) 
        x
        (-x))

(define (sqrt x)
    (define (update guess)
        (if (= square guess) x
            guess)
        (update (average guess (/ x guess))))
    (update 1)
)


Lambda expression
(define (plus4 x) x + 4)
(define plus4 (Lambda (x) (+ x 4)))

Cond & Begin
cond behaves like if-elif-else expression in python
begin combines multiple expressions into one

(cond ((> x 10) (print 'big))
      ((> x 5)  (print 'medium))
      (else     (print 'small))
)

(print
(cond ((> x 10) 'big)
      ((> x 5)  'medium)
      (else     'small)
))

(if (> x 10)
    (begin
        (print 'big)
        (print 'guy))
    (beging
        (print 'small)
        (print 'fry)))

Let expression
let binds symbols to values temporarily

(define c (let ((a 3) (b 4))
        (sqrt (+ (* a a) (* b b))))
)


1 3 1 3
s.rest.first.rest